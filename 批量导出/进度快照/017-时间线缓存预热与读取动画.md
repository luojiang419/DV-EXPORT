# 017 时间线缓存预热与读取动画

## 已完成内容

- 新增运行时 `prepareTimelineCache()` 桥接方法，启动后主动读取项目时间线并复用现有项目级缓存
- 前端启动时自动触发时间线缓存预热，时间线列表区域显示“正在读取时间线信息...”加载动画
- 文件夹点击仍保留原有读取逻辑，并继续通过 `ensureProjectCaches()` 做缓存兜底
- 调整 Vitest 配置，排除 `backup`、`build`、`dist`，避免备份目录中的测试被重复执行
- 已完成阶段源码备份：`backup/009-时间线缓存预热前`
- 已构建新版本：`dist/v0.1.10`
- 验证通过：`npm run typecheck`、`npm test`、`npm run build`

## 当前修改模块

- `plugin-runtime/preload.js`
- `src/app/App.tsx`
- `src/ui/components/TimelineList.tsx`
- `src/types/models.ts`
- `src/types/resolve.ts`
- `vitest.config.ts`

## 待办清单

- 在 Resolve 实机里验证启动期是否出现“读取时间线信息”动画
- 验证大项目首次点击文件夹是否不再明显卡死
- 验证切换多个文件夹时是否复用缓存并保持时间线列表正确
- 用 `dist/v0.1.10` 覆盖安装到当前 Resolve 插件目录后做完整流程验证

## 下一步

安装并运行 `dist/v0.1.10`，重点实测大项目首次进入插件和首次点击文件夹时的响应表现。
