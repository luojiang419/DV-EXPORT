# 018 安装 v0.1.10 并启动 Resolve

## 已完成内容

- 已将当前 ProgramData 插件目录中的 `v0.1.9` 备份到 `backup/010-安装v0.1.10前已安装插件/com.dvexport.batch-export`
- 已用 `dist/v0.1.10/com.dvexport.batch-export` 覆盖安装到 Resolve Workflow Integration 插件目录
- 已确认安装目录 `package.json` 版本为 `0.1.10`
- 已确认安装目录包含 `WorkflowIntegration.node`
- 已启动 DaVinci Resolve，当前检测到 `Resolve` 进程和窗口

## 当前修改模块

- 未修改源码
- 部署目标：`C:\ProgramData\Blackmagic Design\DaVinci Resolve\Support\Workflow Integration Plugins\com.dvexport.batch-export`
- 备份目标：`backup/010-安装v0.1.10前已安装插件/com.dvexport.batch-export`

## 待办清单

- 在 Resolve 中打开 `Workspace -> Workflow Integrations -> 达芬奇批量导出`
- 验证插件启动时是否显示“正在读取时间线信息...”加载动画
- 在大项目中验证首次点击文件夹是否不再明显卡死
- 连续切换多个文件夹，确认时间线列表刷新、单选、多选、Shift 连选正常
- 做一次非破坏性的导出流程检查：选择时间线、查看命名预览、确认导出按钮状态

## 下一步

请在已启动的 Resolve 界面中打开插件并完成实机 UI 验证；若出现异常，可用 `backup/010-安装v0.1.10前已安装插件` 回滚到 `v0.1.9`。
