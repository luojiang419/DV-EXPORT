# 002 Resolve 集成骨架

## 已完成内容

- 建立 `main.js + preload.js` 插件运行时结构
- 接入 `WorkflowIntegration.node` 初始化逻辑
- 完成 Resolve Studio / 版本号 / 当前工程读取
- 暴露 bridge：环境信息、媒体池树、时间线列表、导出选项、目录选择、批量导出

## 当前修改模块

- `plugin-runtime/`
- `src/plugin/`
- `src/types/`

## 待办清单

- 完成媒体池文件夹树 UI
- 完成时间线多选
- 完成导出设置 UI 和批量导出结果面板
- 完成测试与构建

## 下一步

接入文件夹树、时间线列表和选择逻辑。
