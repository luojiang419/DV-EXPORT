# 011 导出设置失败修复

## 已完成内容

- 排查到导出阶段失败点位于 `project.SetRenderSettings(...)`
- 修复导出设置对象中可选音频/Alpha 字段无值时仍被传入 Resolve 的问题
- 将导出设置改为“基础设置 -> 分辨率 -> 帧率 -> 音频参数 -> Alpha”分步应用
- 失败原因由通用提示改为具体参数项提示，便于定位是分辨率、帧率还是音频设置不被当前格式/编码器接受
- 更新相关单测并通过验证
- 重新构建生成 `dist/v0.1.7`
- 已覆盖安装到当前 Resolve 插件目录

## 当前修改模块

- `plugin-runtime/preload.js`
- `src/core/renderSettings.ts`
- `tests/renderSettings.test.ts`
- `docs/版本说明.md`

## 待办清单

- 在 Resolve 中重新打开插件并执行一次真实导出
- 观察失败提示是否已从通用文案变成具体参数原因
- 若仍失败，记录当前所选格式、编码器、分辨率、帧率，用于继续收敛兼容性问题

## 下一步

先在你当前工程里再试一次导出；如果失败，直接把插件底部结果区的新报错原文发我，我可以继续精准收缩到具体参数。
