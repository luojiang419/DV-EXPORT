# 015 帧率同步与设置持久化完成

## 已完成内容

- 排查出导出报错“当前格式/编码器不接受帧率 24 fps。”的主要原因是插件前端默认帧率固定为 `24`
- 选择时间线后会自动读取该时间线帧率并应用到导出设置
- 点击左侧文件夹扫描时间线时新增“正在扫描时间线...”转圈加载遮罩
- 导出目录、命名模板、格式、编码器、分辨率、帧率和音视频开关已支持本地持久化
- 新增持久化单测，完整测试通过
- 完成类型检查并构建产物到 `dist/v0.1.8`

## 当前修改模块

- `plugin-runtime/preload.js`
- `src/app/App.tsx`
- `src/ui/components/TimelineList.tsx`
- `src/core/exportSettingsStorage.ts`
- `src/styles/app.css`
- `src/types/models.ts`
- `tests/exportSettingsStorage.test.ts`
- `docs/版本说明.md`

## 待办清单

- 在 Resolve 实机里验证：单条时间线选择后帧率是否自动切换
- 在 Resolve 实机里验证：点击媒体池文件夹时 loading 遮罩是否能稳定先显示
- 在 Resolve 实机里验证：重开插件后导出设置是否正确恢复

## 下一步

优先做一次 Resolve 内实机回归，重点看 `24 fps` 报错是否消失，以及多条不同帧率时间线批量导出时是否还需要进一步细化策略。
