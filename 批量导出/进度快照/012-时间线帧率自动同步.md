# 012 时间线帧率自动同步

## 已完成内容

- 定位到导出报错的主要原因是前端默认帧率固定为 `24 fps`
- 为时间线扫描结果补充 `frameRate` 字段
- 选择时间线后自动将该时间线帧率回填到导出设置面板
- 保证当前导出格式选项列表里不存在该帧率时，前端仍能正确显示并保留该值

## 当前修改模块

- `plugin-runtime/preload.js`
- `src/app/App.tsx`
- `src/types/models.ts`

## 待办清单

- 为文件夹扫描增加显式加载动画
- 为导出设置增加本地持久化
- 跑完整测试并构建新版本

## 下一步

继续处理时间线扫描期间的 loading 提示，减少点击文件夹后的空白等待感。
