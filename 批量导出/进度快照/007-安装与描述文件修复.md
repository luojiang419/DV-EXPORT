# 007 安装与描述文件修复

## 已完成内容

- 对照 Resolve 自带 `SamplePlugin` 检查插件描述文件格式
- 修正 `manifest.xml` 根节点与 `FilePath`
- 修复构建脚本在 Windows Node 下无法自动复制 `WorkflowIntegration.node` 的问题
- 重新构建生成 `dist/v0.1.4`
- 重新安装到 Resolve 系统插件目录

## 当前修改模块

- `plugin-runtime/manifest.xml`
- `scripts/build-plugin.mjs`
- `docs/版本说明.md`

## 待办清单

- 重启 Resolve 验证插件菜单是否出现
- 若仍不出现，检查 Resolve 日志与插件加载报错
- 如菜单出现，再继续验证插件运行时桥接和页面加载

## 下一步

重启 Resolve，进入 `Workspace -> Workflow Integrations` 检查插件是否已显示。
