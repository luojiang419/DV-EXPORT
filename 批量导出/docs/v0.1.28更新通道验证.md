# v0.1.28 更新通道验证

本版本用于验证从已安装的 v0.1.27 到 v0.1.28 的完整自动更新链路：

1. GitHub Actions 根据 Latest Release 自动递增版本。
2. 云端执行测试、类型检查、Windows 安装包构建和资产验收。
3. 客户端检测 Latest Release，下载 ZIP 并校验 SHA-256。
4. 独立更新器退出旧版、事务式覆盖安装并重启。

安装后除版本号外，还应包含 `updater/release-channel.json`，以证明实际更新的是新发布载荷，而不是仅修改本地版本记录。
