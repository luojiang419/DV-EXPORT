# 已知限制

- Resolve 官方公开 API 对 Deliver 高级细项的覆盖并不完整，首期通过“基础预设 + 核心参数覆盖”的方式工作。
- 如果当前 Resolve 版本没有暴露 `Timeline.GetMediaPoolItem()`，同名时间线在极端情况下可能退化为按名称匹配。
- 本项目未清空既有 Render Queue；如果用户已有同名输出路径，Resolve 自身的覆盖策略仍由当前预设和软件设置决定。
- 当前未实现子文件夹递归显示时间线。
